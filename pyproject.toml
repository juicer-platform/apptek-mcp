[project]
name = "apptek-mcp"
version = "0.1.0"
description = "Apptek MCP server for ASR, TTS, and MT APIs"
authors = ["Bob Milani <bobmilani@example.com>"]
requires-python = ">=3.8"

[tool.uv]
# uv-specific configuration can be added here if needed

[build-system]
requires = ["uv>=0.1.0"]
build-backend = "uv.build"
